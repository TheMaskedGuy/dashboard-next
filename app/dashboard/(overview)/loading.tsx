import DashboardSkeleton from '@/app/ui/skeletons';
// Naming (overview) as (anything) works! () are the key symbol
//names can be anything
export default function Loadinggg() {
  return <DashboardSkeleton />;
}
